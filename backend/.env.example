# C-Hub HR Backend Environment Variables

PORT=5000
NODE_ENV=development

# Database Configuration (Hostinger Compatible)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=secret
DB_NAME=chub_hr
DB_PORT=3306

# Authentication configuration
JWT_SECRET=chub_super_secret_jwt_key_2026_creating_wow_world
JWT_EXPIRES_IN=8h

# Upload Directory
UPLOAD_DIR=./uploads
